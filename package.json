{
  "name": "explainthiserror-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to analyze CI errors with ExplainThisError",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --minify",
    "format": "prettier -w ."
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "@vercel/ncc": "^0.38.1"
  }
}
